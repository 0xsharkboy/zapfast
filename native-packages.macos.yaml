# Build the complete app input in the native macOS release job first.
schema: 1
tool:
  version: 0.5.1
  nfpm: 2.47.0
nfpm:
  name: zapfast
  maintainer: Carmine Paolino <carmine@paolino.me>
  description: ZapFast macOS application
  license: MIT
release:
  repository: crmne/zapfast
  prereleases: true
targets:
  macos-universal:
    platform: macos
    arch: universal
    formats: [dmg]
    input:
      kind: directory
      local: dist/macos-input
    native:
      command: [ruby, packaging/macos/dmg.rb, '@PAYLOAD@', '@PACKAGE@']
      output: 'zapfast-@TAG@-macos-universal.dmg'
