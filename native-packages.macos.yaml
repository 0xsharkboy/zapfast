# Build the complete app input in the native macOS release job first.
schema: 1
tool:
  version: 0.5.0
  nfpm: 2.47.0
nfpm:
  name: fastsapp
  maintainer: Carmine Paolino <carmine@paolino.me>
  description: FastsApp macOS application
  license: MIT
release:
  repository: crmne/fastsapp
  prereleases: true
targets:
  macos-universal:
    platform: macos
    arch: universal
    formats: [dmg]
    input:
      kind: directory
      local: dist/macos-input
    native:
      command: [ruby, packaging/macos/dmg.rb, '@PAYLOAD@', '@PACKAGE@']
      output: 'fastsapp-@TAG@-macos-universal.dmg'
